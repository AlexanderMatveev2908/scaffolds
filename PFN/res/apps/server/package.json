{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install_pkg": "poetry install",
    "dev": "poetry run uvicorn src.server:app --reload --reload-dir src/ --host 0.0.0.0 --port 3000 --log-level warning",
    "check": "echo 'start check 🕰️'&& poetry run mypy src || { echo \"❌ check failed\"; return 1; } && echo \"check passed ✅\"",
    "test": "pytest",
    "build": "poetry build && mkdir -p app_wheel && cp .env app_wheel/ && cd app_wheel && python -m venv .venv && .venv/bin/pip install ../dist/server-*.whl",
    "start": "cd app_wheel && .venv/bin/gunicorn src.server:app -w 8 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:3000"
  }
}
